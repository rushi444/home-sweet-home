import { FirebaseAuth } from 'src/components/FirebaseAuth'
import { Layout } from 'src/components/Layout'

const Auth = () => {
  return (
    <Layout>
      <FirebaseAuth />
    </Layout>
  )
}

export default Auth
